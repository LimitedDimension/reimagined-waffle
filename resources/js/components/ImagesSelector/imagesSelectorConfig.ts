/**
 * ImagesSelector component config data
 */
export const imagesSelectorConfig = {
    buttonTitles: {
        addImageButtonTitle: "Add image"
    },
    files: {
        multiple: true,
        accept: [
            "image/png",
            "image/gif",
            "image/jpeg",
        ]
    }
}
